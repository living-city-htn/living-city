/**
 * 3D's slot in the app. Product's seam at apps/web/components/city picks this
 * export up automatically — see the note at the top of that file.
 */
export { default as CityScene } from './CityScene'
