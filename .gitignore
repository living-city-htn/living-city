node_modules/
.pnpm-store/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

.env
.env.local
.env.*.local
!.env.example

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

.vercel/
*.log
npm-debug.log*
pnpm-debug.log*

# Pipeline run output (post-analysis.json, plans.json, community-state.json)
packages/pipeline/out/
# Secrets: everything local, except the committed template (docs/05 section 8).
.env*
!.env.example
# Huawei access approval is local and may contain an API credential.
HUAWEI.MD

# Agent tooling dropped in by the Neon/Vercel integration. Machine-generated and
# per-person, so it does not belong in a four-person shared repo.
.agents/
.claude/skills/
skills-lock.json

# Source photos for the seed posts. The resized copies live in
# apps/web/public/seed; the originals are too big to clone.
dummy_post_images/
