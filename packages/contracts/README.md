# @living-city/contracts

TYPES ONLY. Every shape in docs/02 section 7 and docs/03, as Zod schemas. The seam: if it compiles, everyone speaks the same language. Changes need two approvals, all four after Gate 2.

**Owner: Pipeline**

Per `docs/05-team-workflow.md` section 2, only the owner edits this package directly.
Anyone else changes it through a PR the owner reviews.
